const PlatformCommon = require('./platform.common');

/**
* This is a hollow shell of what a page should be. It's this way because we
* need to be able to use the PlatformCommon elements
**/
class PlatformPage  extends PlatformCommon {
}

module.exports = new PlatformPage ;
